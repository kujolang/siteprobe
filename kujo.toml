[package]
name = "siteprobe"
version = "0.2.0"
entry = "src/main.kujo"
description = "Deterministic, bounded website intelligence with Kujo validation"

[dependencies]

[scripts]
test = "kujo run tests/siteprobe_tests.kujo"
