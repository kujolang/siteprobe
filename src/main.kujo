from src.siteprobe import dispatch, diagnostic

/// Native Kujo product entrypoint. Host mechanisms remain capability-gated.
func main() {
    mut result := 1
    try {result = dispatch(args())} except err {diagnostic("SiteProbe: " + to_string(err))}
    exit(result)
}
main()
